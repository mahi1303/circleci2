version: 2.1

jobs:
  build_and_test:
    docker:
      - image: cimg/node:18.17
    steps:
      - checkout

      - run:
          name: Install dependencies
          command: npm install   # ✅ use this instead of npm ci

      - run:
          name: Build the app
          command: npm run build

      - run:
          name: Run placeholder tests
          command: echo "No tests defined. Skipping tests..."

workflows:
  build:
    jobs:
      - build_and_test

